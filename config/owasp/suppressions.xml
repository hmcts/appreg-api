<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
  <!--Please add all the false positives under the below section-->
  <suppress>
    <notes>False Positive
      <![CDATA[file name: jackson-databind-2.15.2.jar]]>
    </notes>
    <packageUrl regex="true">^pkg:maven/com\.fasterxml\.jackson\.core/jackson\-databind@.*$</packageUrl>
    <cve>CVE-2023-35116</cve>
  </suppress>

  <suppress>
    <notes>We do not expose a SMTP interface
    </notes>
    <cve>CVE-2025-7962</cve>
  </suppress>

  <!-- We do not use the nu html checker library -->
  <suppress>
    <notes>
      False positive: CVE applies to Nu HTML Checker (validator.nu).
      hibernate-validator-9.0.1.Final does not contain nu.validator classes (inspected on 2026-01-29).
      Investigation: dependency-check matched a generic "validator" CPE.
      Rationale: suppress to avoid noise; will revisit if package is updated or new evidence appears.
      Investigated-by: Michael Estebanez
    </notes>
    <cve>CVE-2025-15104</cve>
  </suppress>

  <suppress until="2026-09-30Z">
    <notes>
      Temporary suppression: CVE-2026-66299 affects only Tomcat's WebSocket chat example,
      which is not included in this Spring Boot deployment. Apache has fixed the issue in
      Tomcat 11.0.25, but that release is not yet available from Maven Central (verified 2026-08-06).
      Remove this suppression and upgrade the explicit tomcat.version override when it is released.
    </notes>
    <packageUrl regex="true">^pkg:maven/org\.apache\.tomcat\.embed/tomcat-embed-(core|websocket)@11\.0\.24$</packageUrl>
    <cve>CVE-2026-66299</cve>
  </suppress>

  <!--End of false positives section -->
</suppressions>
