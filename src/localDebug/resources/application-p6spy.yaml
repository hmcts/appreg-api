spring:
  datasource:
    url: jdbc:p6spy:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DATABASE:appreg-db}
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
  jpa:
    show-sql: false

logging:
  level:
    p6spy: INFO
