description: >
  The uploaded CSDS snapshot is invalid. This includes records with missing required fields and
  rows that violate target-table constraints. Row-level database reasons are written to CSDS_AUDIT.
content:
  application/problem+json:
    schema:
      $ref: '../schemas/common/problem.yaml'
