description: The uploaded CSDS ingest file is larger than the endpoint allows.
content:
  application/problem+json:
    schema:
      $ref: '../schemas/common/problem.yaml'
    examples:
      csds-payload-too-large:
        value:
          type: https://errors.hmcts.net/common/payload-too-large
          title: Payload Too Large
          status: 413
          detail: CSDS ingest file must not be larger than 1MB
