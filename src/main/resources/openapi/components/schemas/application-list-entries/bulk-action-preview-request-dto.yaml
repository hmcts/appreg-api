type: object
description: Request to resolve a global bulk action selection into IDs and row context.
additionalProperties: false
required:
  - action
  - selection

properties:

  action:
    $ref: "./bulk-action-type.yaml"

  selection:
    $ref: "./bulk-action-selection-dto.yaml"
