type: object
description: >
  Request payload to update notes for an Application List Entry whose Application List is closed.
additionalProperties: false
required:
  - additionalNotes

properties:
  additionalNotes:
    type: string
    description: The additional notes to add to append to the Application List Entry.
    example: "Additional notes"
    minLength: 1
