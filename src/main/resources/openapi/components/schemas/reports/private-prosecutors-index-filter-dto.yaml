
type: object
description: >
  Filter criteria for POST /reports/private-prosecutors-index/jobs.
  Both dateFrom and dateTo are required and are treated as inclusive bounds. Blank fields will be rejected,
  nulls should be used instead.
additionalProperties: false
required:
  - dateFrom
  - dateTo

properties:

  dateFrom:
    type: string
    format: date
    description: Inclusive start date (YYYY-MM-DD).
    example: "2025-10-01"

  dateTo:
    type: string
    format: date
    description: Inclusive end date (YYYY-MM-DD).
    example: "2025-10-31"

  location:
    $ref: './legacy-report-location.yaml'

  applicantSurname:
    type: string
    description: Applicant surname (case-insensitive contains match).
    example: "Smith"
    maxLength: 100
    minLength: 1
    pattern: '.*\S.*'

  applicantFirstName:
    type: string
    description: Applicant first name (case-insensitive contains match).
    example: "John"
    maxLength: 100
    minLength: 1
    pattern: '.*\S.*'

  applicantOrganisationName:
    type: string
    description: Applicant organisation name (case-insensitive contains match).
    example: "Acme Corp"
    maxLength: 100
    minLength: 1
    pattern: '.*\S.*'

  standardApplicantName:
    type: string
    description: Standard applicant name (case-insensitive contains match).
    example: "Crown Prosecution Service"
    maxLength: 100
    minLength: 1
    pattern: '.*\S.*'

  respondentSurname:
    type: string
    description: Respondent surname (case-insensitive contains match).
    example: "Doe"
    maxLength: 100
    minLength: 1
    pattern: '.*\S.*'

  respondentFirstName:
    type: string
    description: Respondent first name (case-insensitive contains match).
    example: "Jane"
    maxLength: 100
    minLength: 1
    pattern: '.*\S.*'

  respondentOrganisationName:
    type: string
    description: Respondent organisation name (case-insensitive contains match).
    example: "XYZ Ltd"
    maxLength: 100
    minLength: 1
    pattern: '.*\S.*'
