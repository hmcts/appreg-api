parameters:
  - in: path
    name: code
    required: true
    description: Code used to identify the Standard Applicant (case-insensitive).
    schema:
      type: string
      maxLength: 10
    example: "SA"
get:
  operationId: getStandardApplicantByCode
  tags: [standard-applicants]
  summary: Get a specific Standard Applicant by code.
  description: |
    Returns a Standard Applicant record matching the supplied code without applying
    lodgement-date effective filtering. Where multiple records share the same code,
    the current active or latest record is selected using deterministic reference-data ordering.
  responses:
    "200":
      description: Standard Applicant found
      headers:
        Vary:
          description: Response varies by Accept for media-type versioning.
          schema: { type: string, example: Accept }
      content:
        application/vnd.hmcts.appreg.v1+json:
          schema: { $ref: "../../components/schemas/standard-applicants/standard-applicant-get-detail-dto.yaml" }
    "400": { $ref: "../../components/responses/bad-request-error.yaml" }
    "401": { $ref: "../../components/responses/unauthorized-error.yaml" }
    "403": { $ref: "../../components/responses/forbidden-error.yaml" }
    "404": { $ref: "../../components/responses/not-found-error.yaml" }
    "406": { $ref: "../../components/responses/not-acceptable-error.yaml" }
    "500": { $ref: "../../components/responses/internal-server-error.yaml" }
