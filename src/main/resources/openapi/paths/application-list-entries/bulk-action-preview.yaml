post:
  operationId: bulkActionPreview
  tags: [ application-list-entries ]
  summary: Resolve global bulk action selection preview
  description: >
    Resolves the current global Application List Entry selection into entry IDs and row context for
    the next bulk action page, enforcing the configured global bulk action limit.
  requestBody:
    required: true
    content:
      application/vnd.hmcts.appreg.v1+json:
        schema:
          $ref: "../../components/schemas/application-list-entries/bulk-action-preview-request-dto.yaml"
  responses:
    "200":
      description: Bulk action preview for the resolved selection.
      headers:
        Vary:
          description: Response varies by Accept for media-type versioning.
          schema:
            type: string
            example: Accept
      content:
        application/vnd.hmcts.appreg.v1+json:
          schema:
            $ref: "../../components/schemas/application-list-entries/bulk-action-preview-response-dto.yaml"
    "400": { $ref: "../../components/responses/bad-request-error.yaml" }
    "401": { $ref: "../../components/responses/unauthorized-error.yaml" }
    "403": { $ref: "../../components/responses/forbidden-error.yaml" }
    "406": { $ref: "../../components/responses/not-acceptable-error.yaml" }
    "413": { $ref: "../../components/responses/payload-too-large-error.yaml" }
    "500": { $ref: "../../components/responses/internal-server-error.yaml" }
