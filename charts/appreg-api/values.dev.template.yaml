java:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    env_name: 'DEV'
  secrets:
    APPREG_DB_PASSWORD:
      secretRef: 'postgres'
      key: PASSWORD
      disabled: false
